from django.contrib import admin
from django.contrib.auth.models import User

# Using default User admin; no additional models to register for now.

# If you had custom user models or profiles, register them here.
